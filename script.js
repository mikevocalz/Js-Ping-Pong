//get key events
function getKeyPress(e) {
  let key = e.keyCode;
  switch (key) {
    case '38':
      ///
      break;

    case 40:
      ///
      break;
  }
  console.log(key);
}

document.addEventListener('keyup' || 'keydown', getKeyPress);
